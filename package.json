{
  "name": "inputto-firestore",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/verify-pages-build.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^12.12.0"
  },
  "devDependencies": {
    "firebase-tools": "^15.14.0",
    "vite": "^7.2.4"
  }
}
